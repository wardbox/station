apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cert-manager controller stack + CRDs. Pinned so bootstrap is reproducible.
  - https://github.com/cert-manager/cert-manager/releases/download/v1.20.2/cert-manager.yaml
